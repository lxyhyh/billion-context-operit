# Security Policy

## 报告漏洞

请**不要**在公开 issue 中提交安全漏洞。请直接联系维护者，或私下披露。

涉及范围：本插件（billion-context-operit）自身的安全问题——包括不安全的终端命令拼接、路径遍历、配置文件越权读写、敏感信息（API Key）泄漏等。

## 处理流程

1. 确认漏洞与影响范围
2. 修复并发布补丁版本
3. 修复后公开披露

## 已知安全设计

- 本插件**不保存** API Key（由 Operit Provider 持有并透传）。
- 服务进程仅监听 `127.0.0.1`，不对外网开放。
- 终端操作全部走可见 session（`Tools.System.terminal`），不使用隐藏执行。
